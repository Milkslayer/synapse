# Copy to `.env` (alongside docker-compose.yml) and edit any value you want
# to override. Anything left commented falls back to the default in
# docker-compose.yml.

# SYNAPSE_PORT=3004
# SYNAPSE_DB_PATH=/data/synapse_v2.db
# SYNAPSE_INSTANCE=synapse-v2
